apiVersion: v1
kind: Service
metadata:
  name: devops-kubernetes-api-service
  labels:
    app: kubernetes-demo-api   # <-- changed
spec:
  selector:
    app: kubernetes-demo-api   # <-- changed
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort